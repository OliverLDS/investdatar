% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instrument_catalog.R
\name{get_instrument_catalog}
\alias{get_instrument_catalog}
\title{Get the Instrument Catalog}
\usage{
get_instrument_catalog(
  catalog_path = .instrument_catalog_seed_path(),
  yahoo_registry_path = get_yahoofinance_seed_registry_path(),
  validate = TRUE
)
}
\arguments{
\item{catalog_path}{Path to a catalog JSON file. Defaults to the packaged
seed.}

\item{yahoo_registry_path}{Path to the Yahoo Finance sync registry. Defaults
to the packaged seed registry so the catalog is usable without a runtime
configuration file. Pass a runtime registry path to validate that runtime
registry explicitly.}

\item{validate}{Logical. When \code{TRUE} (the default), validate the catalog and
Yahoo registry before returning it.}
}
\value{
A \code{data.table} with one row per instrument. \code{provider_identifiers},
\code{primary_source}, \code{fallback_sources}, and \code{supported_intervals} are list
columns that preserve their JSON mapping or array structure.
}
\description{
Returns factual, provider-neutral market-instrument metadata for downstream
consumers. It deliberately excludes editorial, ranking, simulation, and
execution fields. \code{canonical_symbol} is a consumer identity, not a provider
symbol. Current Yahoo local-cache access and synchronization resolve through
\code{provider_identifiers$yahoo}; do not pass \code{canonical_symbol} directly to
\code{get_completed_local_quantmod_OHLC()}. \code{primary_source} describes intended
routing; actual row-level provenance remains the \code{source} recorded by
synchronization.
}
