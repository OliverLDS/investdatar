% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnh_cache_diagnostic.R
\name{diagnose_cnh_cache_integrity}
\alias{diagnose_cnh_cache_integrity}
\title{Diagnose CNH/USD Cache Differences}
\usage{
diagnose_cnh_cache_integrity(
  local_path = NULL,
  output_dir = NULL,
  from = NULL,
  to = NULL,
  overlap_days = 35L
)
}
\arguments{
\item{local_path}{Yahoo Finance cache directory. Defaults to the configured
Yahoo Finance data path.}

\item{output_dir}{Directory for diagnostic artifacts. Defaults to
\verb{<local_path>/_audits/cnh_cache_diagnostic}.}

\item{from}{Optional inclusive date. Defaults to the recent cache overlap.}

\item{to}{Optional inclusive date. Defaults to the recent cache overlap.}

\item{overlap_days}{Number of calendar days used when \code{from} and \code{to} are
both omitted.}
}
\value{
A list with \code{summary}, field-level \code{differences}, and artifact paths.
}
\description{
Compares the read-only local \code{CNH=X} cache with a fresh TLS-verified
Eastmoney \verb{133.USDCNH} pull. The resulting JSON and CSV artifacts contain
field-level differences, source timestamps, trading-date labels, provenance,
tolerance calculations, and conservative classifications. This function
never modifies cache or synchronization files and does not create repair
candidates.
}
\details{
The Yahoo and Eastmoney identifiers are both interpreted as USD/CNH, meaning
CNH units per one USD. Yahoo's \code{CNH=X} is retained as the provider symbol;
Eastmoney's \verb{133.USDCNH} is the corresponding provider symbol. Eastmoney
daily labels are reported with the provider's Asia/Shanghai market-date
convention; its HTTP response does not expose a bar timezone explicitly.
}
