% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/yahoo_overlap_audit.R
\name{audit_yahoofinance_recent_overlap}
\alias{audit_yahoofinance_recent_overlap}
\title{Audit Recent Yahoo Finance Cache Overlaps}
\usage{
audit_yahoofinance_recent_overlap(
  registry = get_yahoofinance_registry(),
  local_path = NULL,
  output_dir = NULL,
  overlap_days = 35L,
  as_of = Sys.time(),
  fallback_corroboration = c("on_issue", "none", "all"),
  require_catalog_metadata = TRUE,
  max_attempts = 3L,
  retry_delay_seconds = 1
)
}
\arguments{
\item{registry}{Yahoo Finance registry table. Defaults to the configured
runtime registry.}

\item{local_path}{Yahoo Finance cache directory. Defaults to the configured
Yahoo Finance data path.}

\item{output_dir}{Audit artifact directory. Defaults to
\verb{<local_path>/_audits/yahoo_recent_overlap}.}

\item{overlap_days}{Number of recent calendar days to inspect. Defaults to
\code{35}.}

\item{as_of}{UTC time used to exclude the current market-calendar date.
For \code{FX_24_5}, Yahoo currency chart epochs are retained under their UTC
cache-date convention; the response's exchange timezone and session-boundary
metadata are used only to classify a narrow Friday session-label gap.}

\item{fallback_corroboration}{One of \code{"on_issue"}, \code{"none"}, or \code{"all"}.
The default uses only existing TLS-verified Eastmoney mappings for
\verb{000300.SS} and \code{CNH=X}; BaoStock is never queried.}

\item{require_catalog_metadata}{When \code{TRUE}, the default, unclassified
registry symbols are skipped rather than assigned inferred metadata.}

\item{max_attempts}{Maximum Yahoo attempts per catalog-backed instrument.}

\item{retry_delay_seconds}{Initial retry delay in seconds.}
}
\value{
A list with \code{summary}, \code{findings}, and JSON/CSV \code{artifact_paths}.
}
\description{
Compare completed daily rows in catalog-backed Yahoo Finance caches with a
fresh Yahoo overlap. The audit writes versioned JSON and CSV artifacts only;
it never synchronizes, repairs, or modifies a cache. Registry symbols without
catalog metadata produce explicit \code{skipped_missing_catalog_metadata} findings.
}
