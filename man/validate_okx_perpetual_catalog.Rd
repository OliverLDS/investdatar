% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instrument_catalog.R
\name{validate_okx_perpetual_catalog}
\alias{validate_okx_perpetual_catalog}
\title{Validate the 4-Hour OKX Perpetual Catalog}
\usage{
validate_okx_perpetual_catalog(
  catalog_path = .instrument_catalog_seed_path(),
  local_path = NULL,
  as_of = Sys.time()
)
}
\arguments{
\item{catalog_path}{Path to a catalog JSON file. Defaults to the packaged
seed.}

\item{local_path}{Optional local OKX candle-cache directory. Supplying it
enables catalog-to-cache validation.}

\item{as_of}{UTC time used to exclude cache rows at or after the cutoff.}
}
\value{
A list with \code{valid}, \code{errors}, \code{catalog}, and \code{cache_status} data
tables.
}
\description{
Validates the provider-neutral catalog and returns its active or inactive
4-hour OKX perpetual-swap subset. When \code{local_path} is supplied, every
active instrument must have a non-empty local cache with a finite completed
OHLC row. OKX cache ingestion already excludes unconfirmed candles, so this
check never treats an in-progress candle as usable.
}
