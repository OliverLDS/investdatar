% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_local.R
\name{sync_local_data_partitioned}
\alias{sync_local_data_partitioned}
\title{Synchronize Monthly Partitioned Local Data}
\usage{
sync_local_data_partitioned(
  new_data,
  local_file_path,
  time_col,
  key_cols,
  order_cols = key_cols,
  source_utime = NULL,
  local_updated_at = Sys.time(),
  tz = "UTC"
)
}
\arguments{
\item{new_data}{A \code{data.table}-compatible object.}

\item{local_file_path}{Output \code{.rds} file path.}

\item{time_col}{Name of the timestamp column used for monthly partitioning.}

\item{key_cols}{Character vector of key columns used for de-duplication.}

\item{order_cols}{Character vector used to sort the merged data.}

\item{source_utime}{Optional upstream update time recorded in metadata.}

\item{local_updated_at}{Optional local update time override.}

\item{tz}{Time zone used to determine month boundaries.}
}
\value{
A sync result list. Its \code{data} element contains the touched rows,
rather than the complete potentially large dataset.
}
\description{
Upserts only monthly partitions touched by \code{new_data}. On the first
partitioned sync, an existing monolithic RDS cache is copied into monthly
partitions so callers can opt in without a separate migration step.
}
