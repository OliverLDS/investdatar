% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instrument_catalog.R
\name{validate_instrument_catalog}
\alias{validate_instrument_catalog}
\title{Validate the Instrument Catalog}
\usage{
validate_instrument_catalog(
  catalog_path = .instrument_catalog_seed_path(),
  yahoo_registry_path = get_yahoofinance_seed_registry_path()
)
}
\arguments{
\item{catalog_path}{Path to a catalog JSON file. Defaults to the packaged
seed.}

\item{yahoo_registry_path}{Path to the Yahoo Finance sync registry. Defaults
to the packaged seed registry so the catalog is usable without a runtime
configuration file. Pass a runtime registry path to validate that runtime
registry explicitly.}
}
\value{
A list with \code{valid}, an \code{errors} data table, and a \code{catalog} data
table. The catalog retains nested provider mappings as list columns.
}
\description{
Validates the package-owned, provider-neutral instrument catalog and its
Yahoo identifier and fallback projection against a Yahoo Finance registry.
The Yahoo registry remains the sync authority: catalog fallback entries are
validated projections, not instructions for synchronization.
}
\details{
Every public field is required and non-null except that \code{fallback_sources}
may be an empty array. \code{asset_class} must be one of \code{equity},
\code{fixed_income}, \code{commodity}, \code{foreign_exchange}, or \code{cryptocurrency}.
\code{market_calendar} must be one of \code{XNYS}, \code{XSHG}, \code{FX_24_5}, or
\code{CRYPTO_24_7}. \code{provider_identifiers} is a non-empty named list whose
values are non-empty provider symbols. \code{quote_currency} is a three-letter
uppercase currency code; \code{CNH} is permitted as the conventional offshore
Chinese-yuan quote code. \code{price_frequency} and every
\code{supported_intervals} entry are currently limited to \verb{1d}.
Each \code{fallback_sources} object has exactly \code{provider} and \code{symbol} fields;
pairs must be unique and ordered lexicographically by provider then symbol.
}
